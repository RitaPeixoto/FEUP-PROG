/*Resolva o problema 3.5 usando uma struct para representar uma fracção: struct Fraction {    int numerator;     int denominator; }; 
O protótipo das funções readFracc()e reduceFracc() passará a ser Fraction readFracc()e  Fraction reduceFracc(Fraction f), respectivamente. 
O protótipo das outras funções desenvolvidas deverá ser modificado de modo semelhante. 
 */